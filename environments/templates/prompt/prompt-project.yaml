apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prompt
  namespace: argocd
spec:
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
  destinations:
  - name: '*'
    namespace: '*'
    server: '*'
  namespaceResourceWhitelist:
  - group: '*'
    kind: '*'
  roles:
  - name: admin
    description: Admin privileges to prompt
    policies:
    - p, proj:prompt:admin, repositories, *, prompt/*, allow
    groups:
    - role:admin
  sourceRepos:
  - '*'
