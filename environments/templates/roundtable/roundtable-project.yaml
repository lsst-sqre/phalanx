apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: roundtable
  namespace: argocd
spec:
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
  destinations:
  - name: '*'
    namespace: '*'
    server: '*'
  namespaceResourceWhitelist:
  - group: '*'
    kind: '*'
  roles:
  - name: admin
    description: Admin privileges to roundtable
    policies:
    - p, proj:roundtable:admin, repositories, *, roundtable/*, allow
    groups:
    - role:admin
  sourceRepos:
  - '*'
