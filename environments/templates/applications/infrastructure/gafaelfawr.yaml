{{- if .Values.applications.gafaelfawr -}}
apiVersion: v1
kind: Namespace
metadata:
  name: "gafaelfawr"
  {{- if .Values.defaultComputeClass | or .Values.namespaceLabels.gafaelfawr }}
  labels:
  {{- if .Values.defaultComputeClass }}
    cloud.google.com/default-compute-class: {{ .Values.defaultComputeClass }}
  {{- end}}
  {{- with .Values.namespaceLabels.gafaelfawr }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "gafaelfawr"
  namespace: "argocd"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "gafaelfawr"
    server: "https://kubernetes.default.svc"
  project: "infrastructure"
  source:
    path: "applications/gafaelfawr"
    repoURL: {{ .Values.repoUrl | quote }}
    targetRevision: {{ or (index .Values "revisions" "gafaelfawr") .Values.targetRevision | quote }}
    helm:
      parameters:
        - name: "global.baseUrl"
          value: "https://{{ .Values.fqdn }}"
        - name: "global.environmentName"
          value: {{ .Values.name | quote }}
        - name: "global.host"
          value: {{ .Values.fqdn | quote }}
        - name: "global.vaultSecretsPath"
          value: {{ .Values.vaultPathPrefix | quote }}
      valueFiles:
        - "values.yaml"
        - "values-{{ .Values.name }}.yaml"
{{- end -}}
