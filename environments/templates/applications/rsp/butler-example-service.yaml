{{- if (index .Values "applications" "butler-example-service") -}}
apiVersion: v1
kind: Namespace
metadata:
  name: "butler-example-service"
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "butler-example-service"
  namespace: "argocd"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "butler-example-service"
    server: "https://kubernetes.default.svc"
  project: "rsp"
  source:
    path: "applications/butler-example-service"
    repoURL: {{ .Values.repoUrl | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    helm:
      parameters:
        - name: "global.host"
          value: {{ .Values.fqdn | quote }}
        - name: "global.baseUrl"
          value: "https://{{ .Values.fqdn }}"
        - name: "global.butlerServerRepositories"
          value: {{ .Values.butlerServerRepositories | toJson | b64enc }}
        - name: "global.vaultSecretsPath"
          value: {{ .Values.vaultPathPrefix | quote }}
      valueFiles:
        - "values.yaml"
        - "values-{{ .Values.name }}.yaml"
{{- end -}}