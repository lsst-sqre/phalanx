 {{- if (index .Values "applications" "bigquery-kafka") -}}
apiVersion: v1
kind: Namespace
metadata:
  name: "bigquery-kafka"
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "bigquery-kafka"
  namespace: "argocd"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "bigquery-kafka"
    server: "https://kubernetes.default.svc"
  project: "rsp"
  source:
    path: "applications/bigquery-kafka"
    repoURL: {{ .Values.repoUrl | quote }}
    targetRevision: {{ or (index .Values "revisions" "bigquery-kafka") .Values.targetRevision | quote }}
    helm:
      parameters:
        - name: "global.environmentName"
          value: {{ .Values.name | quote }}
        - name: "global.host"
          value: {{ .Values.fqdn | quote }}
        - name: "global.repertoireUrl"
          value: "https://{{ .Values.fqdn }}/repertoire"
        - name: "global.vaultSecretsPath"
          value: {{ .Values.vaultPathPrefix | quote }}
      valueFiles:
        - "values.yaml"
        - "values-{{ .Values.name }}.yaml"
{{- end -}}
