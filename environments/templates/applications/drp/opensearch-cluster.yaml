{{- if (index .Values "applications" "opensearch-cluster") -}}
apiVersion: v1
kind: Namespace
metadata:
  name: "opensearch-cluster"
  {{- if .Values.defaultComputeClass }}
  labels:
    cloud.google.com/default-compute-class: {{ .Values.defaultComputeClass | quote }}
  {{- end}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "opensearch-cluster"
  namespace: "argocd"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "opensearch-cluster"
    server: "https://kubernetes.default.svc"
  project: "drp"
  source:
    path: "applications/opensearch-cluster"
    repoURL: {{ .Values.repoUrl | quote }}
    targetRevision: {{ or (index .Values "revisions" "opensearch-cluster") .Values.targetRevision | quote }}
    helm:
      parameters:
        - name: "global.baseUrl"
          value: "https://{{ .Values.fqdn }}"
        - name: "global.environmentName"
          value: {{ .Values.name | quote }}
        - name: "global.host"
          value: {{ .Values.fqdn | quote }}
        - name: "global.repertoireUrl"
          value: "https://{{ .Values.fqdn }}/repertoire"
        - name: "global.vaultSecretsPath"
          value: {{ .Values.vaultPathPrefix | quote }}
      valueFiles:
        - "values.yaml"
        - "values-{{ .Values.name }}.yaml"
{{- end -}}