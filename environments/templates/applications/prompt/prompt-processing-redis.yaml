{{- if (index .Values "applications" "prompt-processing-redis") -}}
apiVersion: v1
kind: Namespace
metadata:
  name: "prompt-processing-redis"
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "prompt-processing-redis"
  namespace: "argocd"
  finalizers:
    - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "prompt-processing-redis"
    server: "https://kubernetes.default.svc"
  project: "prompt"
  source:
    path: "applications/prompt-processing-redis"
    repoURL: {{ .Values.repoUrl | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    helm:
      parameters:
        - name: "global.host"
          value: {{ .Values.fqdn | quote }}
        - name: "global.baseUrl"
          value: "https://{{ .Values.fqdn }}"
        - name: "global.vaultSecretsPath"
          value: {{ .Values.vaultPathPrefix | quote }}
      valueFiles:
        - "values.yaml"
        - "values-{{ .Values.name }}.yaml"
{{- end -}}