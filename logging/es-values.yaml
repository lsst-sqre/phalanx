image:
  repository: "amazon/opendistro-for-elasticsearch"
  tag: "0.8.0"
master:
  persistence:
    enabled: false
data:
  replicas: 6
  persistence:
    enabled: false
    #name: 'pvc-lspelk'
    #size: '100Gi'
  heapSize: "1g"
  resources:
    limits:
      cpu: "2"
      memory: 8G
    requests:
      cpu: "2"
      memory: 7G
client:
  replicas: 12
  heapSize: "2g"
  resources:
    limits:
      cpu: "2"
      memory: 8G
    requests:
      cpu: "2"
      memory: 7G
cluster:
  config:
    opendistro_security.disabled: true

