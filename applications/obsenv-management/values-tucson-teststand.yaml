obsenv-api:
  image:
    repository: mareuter/obsenv-api
    tag: develop
    pullPolicy: Always
  config:
    useFakeObsenvManager: true

obsenv-ui:
  image:
    repository: mareuter/obsenv-ui
    tag: develop
    pullPolicy: Always
  config:
    pathPrefix: /obsenv-ui
