apiVersion: v1
kind: Service
metadata:
  name: "obsenv-management"
  labels:
    {{- include "obsenv-management.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "obsenv-management.selectorLabels" . | nindent 4 }}
