---
# Ook endpoints default to anonymous scope
apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "ook"
  labels:
    {{- include "ook.labels" . | nindent 4 }}
config:
  loginRedirect: false
  scopes:
    anonymous: true
  service: "ook"
template:
  metadata:
    name: "ook"
    {{- with .Values.ingress.annotations }}
    annotations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: {{ .Values.ingress.path | quote }}
              pathType: "Prefix"
              backend:
                service:
                  name: "ook"
                  port:
                    number: {{ .Values.service.port }}

---
# Swagger API browser has exec:internal-tools scope and a login redirect
# to ensure that the user is logged in.
apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "ook-apibrowser"
  labels:
    {{- include "ook.labels" . | nindent 4 }}
config:
  loginRedirect: true
  scopes:
    all:
      - "exec:internal-tools"
  service: "ook"
template:
  metadata:
    name: "ook-apibrowser"
    {{- with .Values.ingress.annotations }}
    annotations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "{{ .Values.ingress.path }}/docs"
              pathType: "Prefix"
              backend:
                service:
                  name: "ook"
                  port:
                    number: {{ .Values.service.port }}

---
# APIs that require exec:internal-tools (/ingest and /admin)
apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "ook-admin"
  labels:
    {{- include "ook.labels" . | nindent 4 }}
config:
  loginRedirect: false
  scopes:
    all:
      - "exec:internal-tools"
  service: "ook"
template:
  metadata:
    name: "ook-admin"
    {{- with .Values.ingress.annotations }}
    annotations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "{{ .Values.ingress.path }}/admin"
              pathType: "Prefix"
              backend:
                service:
                  name: "ook"
                  port:
                    number: {{ .Values.service.port }}
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "{{ .Values.ingress.path }}/ingest"
              pathType: "Prefix"
              backend:
                service:
                  name: "ook"
                  port:
                    number: {{ .Values.service.port }}
