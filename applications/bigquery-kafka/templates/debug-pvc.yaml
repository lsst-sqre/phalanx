{{- if .Values.frontend.debug.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bigquery-kafka-debug
  namespace: bigquery-kafka
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: "standard-rwo"
{{- end }}
