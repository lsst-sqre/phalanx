apiVersion: v1
kind: ServiceAccount
metadata:
  name: "bigquery-kafka"
  labels:
    {{- include "bigquery-kafka.labels" . | nindent 4 }}
  {{- if .Values.config.gcpServiceAccount }}
  annotations:
    iam.gke.io/gcp-service-account: {{ .Values.config.gcpServiceAccount | quote }}
  {{- end }}
