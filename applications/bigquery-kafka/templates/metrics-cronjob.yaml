apiVersion: batch/v1
kind: CronJob
metadata:
  name: periodic-metrics
  labels:
    {{- include "bigquery-kafka.labels" . | nindent 4 }}
spec:
  concurrencyPolicy: "Forbid"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "bigquery-kafka.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: "periodic-metrics"
            bigquery-kafka-redis-client: "true"
        spec:
          {{- with .Values.periodicMetrics.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          automountServiceAccountToken: true
          containers:
            - name: periodic-metrics
              command: ["qserv-kafka", "publish-periodic-metrics"]
              env:
              {{- include "bigquery-kafka.envVars" (dict "Values" .Values) | nindent 14 }}
              envFrom:
                - configMapRef:
                    name: "bigquery-kafka"
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              resources:
                {{- toYaml .Values.periodicMetrics.resources | nindent 16 }}
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - "all"
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: "kafka"
                  mountPath: "/etc/bigquery-kafka/ca.crt"
                  readOnly: true
                  subPath: "ssl.truststore.crt"
                - name: "kafka"
                  mountPath: "/etc/bigquery-kafka/user.crt"
                  readOnly: true
                  subPath: "ssl.keystore.crt"
                - name: "kafka"
                  mountPath: "/etc/bigquery-kafka/user.key"
                  readOnly: true
                  subPath: "ssl.keystore.key"
          {{- with .Values.periodicMetrics.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: "Never"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
          {{- with .Values.periodicMetrics.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumes:
            - name: "kafka"
              secret:
                secretName: "bigquery-kafka-access"
  schedule: {{ .Values.periodicMetrics.schedule | quote }}
