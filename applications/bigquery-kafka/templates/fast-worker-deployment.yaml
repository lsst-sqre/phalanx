apiVersion: apps/v1
kind: Deployment
metadata:
  name: "bigquery-kafka-fast-worker"
  labels:
    {{- include "bigquery-kafka.labels" . | nindent 4 }}
spec:
  {{- if not .Values.fastWorker.autoscaling.enabled }}
  replicas: {{ .Values.fastWorker.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bigquery-kafka.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: "fast-worker"
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.fastWorker.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "bigquery-kafka.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: "fast-worker"
        bigquery-kafka-redis-client: "true"
    spec:
      {{- with .Values.fastWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      automountServiceAccountToken: false
      containers:
        - name: "{{ .Chart.Name }}-fast-worker"
          command:
            - "arq"
            - "qservkafka.workers.main.FastWorkerSettings"
          env:
            {{- include "bigquery-kafka.envVars" (dict "Values" .Values) | nindent 12 }}
          envFrom:
          - configMapRef:
              name: "bigquery-kafka"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.fastWorker.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - "all"
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: "kafka"
              mountPath: "/etc/bigquery-kafka/ca.crt"
              readOnly: true
              subPath: "ssl.truststore.crt"
            - name: "kafka"
              mountPath: "/etc/bigquery-kafka/user.crt"
              readOnly: true
              subPath: "ssl.keystore.crt"
            - name: "kafka"
              mountPath: "/etc/bigquery-kafka/user.key"
              readOnly: true
              subPath: "ssl.keystore.key"
      {{- with .Values.fastWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ add .Values.config.resultTimeout 10 }}
      {{- with .Values.fastWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: {{ not .Values.fastWorker.allowRootDebug }}
        runAsUser: 1000
        runAsGroup: 1000
      volumes:
        - name: "kafka"
          secret:
            secretName: "bigquery-kafka-access"
