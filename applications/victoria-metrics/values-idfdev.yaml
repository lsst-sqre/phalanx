vmstack:
  vmsingle:
    spec:
      storage:
        storageClassName: "standard-rwo"

  vmagent:
    spec:
      statefulStorage:
        volumeClaimTemplate:
          spec:
            storageClassName: "standard-rwo"

  alertmanager:
    spec:
      externalURL: https://data-dev.lsst.cloud/alertmanager
      storage:
        volumeClaimTemplate:
          spec:
            storageClassName: "standard-rwo"

