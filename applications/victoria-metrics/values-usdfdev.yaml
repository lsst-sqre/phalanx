vmlogs:
    persistentVolume:
      storageClassName: "wekafs--sdf-k8s01"

vmstack:
  vmsingle:
    spec:
      storage:
        storageClassName: "wekafs--sdf-k8s01"

  vmagent:
    spec:
      statefulStorage:
        volumeClaimTemplate:
          spec:
            storageClassName: "wekafs--sdf-k8s01"
            resources:
              requests:
                storage: "6Gi"

  alertmanager:
    spec:
      externalURL: https://usdf-rsp-dev.slac.stanford.edu/alertmanager
      storage:
        volumeClaimTemplate:
          spec:
            storageClassName: "wekafs--sdf-k8s01"
