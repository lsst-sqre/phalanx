apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "victoria-metrics"
  labels:
    {{- include "victoria-metrics.labels" . | nindent 4 }}
config:
  baseUrl: {{ .Values.global.baseUrl | quote }}
  loginRedirect: true
  scopes:
    all:
      - "exec:admin"
  service: "victora-metrics"
template:
  metadata:
    name: "victoria-metrics"
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
    {{- with .Values.vmSingle.ingress.annotations }}
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "/{{ .Values.vmSingle.pathPrefix }}(/|$)(.*)"
              pathType: "ImplementationSpecific"
              backend:
                # The VictoriaMetrics operator creates this service when a VMSingle CRD
                # is applied
                service:
                  name: "vmsingle-victoria-metrics"
                  port:
                    number: 8429
