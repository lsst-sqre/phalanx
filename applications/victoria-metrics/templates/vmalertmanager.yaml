apiVersion: operator.victoriametrics.com/v1beta1
kind: VMAlertmanager
metadata:
  name: alertmanager
spec:
  externalURL: "{{ .Values.global.baseUrl }}/{{ .Values.vmAlertmanager.pathPrefix }}"
  {{- .Values.vmAlertmanager.spec | toYaml | nindent 2 }}
