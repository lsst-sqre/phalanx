apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "victoria-metrics"
  labels:
    {{- include "victoria-metrics.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/victoria-metrics"
  type: "Opaque"
