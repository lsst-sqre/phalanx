apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "vmagent"
  labels:
    {{- include "victoria-metrics.labels" . | nindent 4 }}
config:
  baseUrl: {{ .Values.global.baseUrl | quote }}
  loginRedirect: true
  scopes:
    all:
      - "exec:internal-tools"
  service: "vmagent"
template:
  metadata:
    name: "vmagent"
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
    {{- with .Values.vmstack.vmagent.gafaelfawr.annotations }}
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "/{{ .Values.vmstack.vmagent.gafaelfawr.pathPrefix }}(/|$)(.*)"
              pathType: "ImplementationSpecific"
              backend:
                service:
                  name: {{ .Values.vmstack.vmagent.gafaelfawr.serviceName | quote }}
                  port:
                    number: 8429
