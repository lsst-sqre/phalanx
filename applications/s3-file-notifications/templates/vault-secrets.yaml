apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "s3-file-notifications"
  labels:
    {{- include "s3-file-notifications.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/s3-file-notifications"
  type: Opaque
