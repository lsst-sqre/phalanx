## Default values.yaml for the Prompt Publication subchart

cluster:
  # -- Name of the Strimzi cluster. Synchronize this with the cluster name in
  # the parent Prompt Kafka chart.
  name: prompt-kafka
