{{- if .Values.config.slackAlerts -}}
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "octavius-secrets"
  labels:
    {{- include "octavius.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/octavius"
  type: Opaque
{{- end }}
