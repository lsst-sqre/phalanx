apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: octavius-db-networkpolicy
spec:
  podSelector:
    matchLabels:
      {{- $labels := include "octavius.selectorLabels" . | fromYaml }}
      {{- $_ := set $labels "component" "db" }}
      {{- toYaml $labels | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    # Allow only API to connect to MongoDB
    - from:
        - podSelector:
            matchLabels:
              {{- $labels := include "octavius.selectorLabels" . | fromYaml }}
              {{- $_ := set $labels "component" "api" }}
              {{- toYaml $labels | nindent 12 }}
      ports:
        - protocol: "TCP"
          port: 27017