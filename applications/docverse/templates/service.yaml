apiVersion: v1
kind: Service
metadata:
  name: "docverse"
  labels:
    {{- include "docverse.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "docverse.selectorLabels" . | nindent 4 }}
