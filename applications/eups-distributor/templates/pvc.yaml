---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "eups-cache-pvc"
spec:
  storageClassName: "standard-rwo"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
