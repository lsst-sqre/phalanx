apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    {{- include "exposure-checker.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "exposure-checker.selectorLabels" . | nindent 4 }}
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
