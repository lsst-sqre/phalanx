apiVersion: v1
kind: Service
metadata:
  name: prompt-pub-headless
spec:
  clusterIP: None
  selector:
    {{- include "prompt-pub.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      name: http