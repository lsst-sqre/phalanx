prompt-proto-service:

  podAnnotations:
    # HACK: disable autoscaling as workaround for DM-41829
    autoscaling.knative.dev/min-scale: "750"
    autoscaling.knative.dev/max-scale: "750"
    edu.stanford.slac.sdf.project/usdf-embargo: "true"
    # Update this field if using latest or static image tag in dev
    revision: "1"

  # HACK: disable autoscaling as workaround for DM-41829
  worker:
    restart: 7  # See comments on DM-49255

  image:
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: 5.1.1

  instrument:
    pipelines:
      # IMPORTANT: don't use flow-style mappings (i.e., {}) in pipelines specs
      # if the result (including any comments) is longer than 72 characters.
      # The config will get corrupted after template substitution.
      # Block-style mappings can have lines of any length.
      main: |-
        - survey: SURVEY
          pipelines:
          - ${PROMPT_PROCESSING_DIR}/pipelines/LSSTCam-imSim/ApPipe.yaml
          - ${PROMPT_PROCESSING_DIR}/pipelines/LSSTCam-imSim/SingleFrame.yaml
          - ${PROMPT_PROCESSING_DIR}/pipelines/LSSTCam-imSim/Isr.yaml
      preprocessing: |-
        - survey: SURVEY
          pipelines: ['${PROMPT_PROCESSING_DIR}/pipelines/LSSTCam-imSim/Preprocessing.yaml']
    calibRepo: s3://repo-test-users/or5
    skymap: "lsst_cells_v1"

  s3:
    imageBucket: repo-test
    endpointUrl: https://sdfembs3.sdf.slac.stanford.edu
    # Do not set AWS_SECRET_ACCESS_KEY and AWS_ACCESS_KEY_ID. Use cred file
    cred_file_auth: True
    auth_env: False
    aws_profile: embargo

  raw_microservice: http://172.24.5.224:8080/presence

  imageNotifications:
    kafkaClusterAddress: prompt-processing2-kafka-bootstrap.kafka:9092
    topic: or5-ppdev-notification

  apdb:
    config: s3://rubin-summit-users/apdb_config/cassandra/pp_apdb_lsstcamimsim-dev.yaml

  alerts:
    topic: "alerts-simulated"

  logLevel: "timer.lsst.activator=DEBUG lsst.diaPipe=VERBOSE lsst.rbClassify=VERBOSE lsst.daf.butler=VERBOSE"

  sasquatch:
    endpointUrl: https://usdf-rsp-dev.slac.stanford.edu/sasquatch-rest-proxy
    auth_env: false

  knative:
    # Resources for 2 workers
    cpuRequest: 1
    cpuLimit: 2
    ephemeralStorageRequest: "16Gi"
    ephemeralStorageLimit: "16Gi"
    memoryRequest: "4Gi"
    memoryLimit: "24Gi"

  containerConcurrency: 2

  fullnameOverride: "prompt-proto-service-lsstcamimsim"
