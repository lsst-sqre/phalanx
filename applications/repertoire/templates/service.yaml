apiVersion: v1
kind: Service
metadata:
  name: "repertoire"
  labels:
    {{- include "repertoire.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "repertoire.selectorLabels" . | nindent 4 }}
