{{- if (or .Values.config.sentry.enabled .Values.config.slackAlerts .Values.config.influxdbDatabases .Values.config.tapServers) -}}
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "repertoire"
  labels:
    {{- include "repertoire.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/repertoire"
  type: Opaque
{{- end }}
