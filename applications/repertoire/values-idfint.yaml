config:
  availableDatasets:
    - "dp02"
    - "dp03"
    - "dp1"
    - "dp2"
    - "prompt"
  hips:
    legacy:
      dataset: "dp1"
  metrics:
    enabled: true
  rules:
    # Override the required scope of times-square. Unfortunately, there is no
    # way to do this at the moment without duplicating the entry.
    times-square:
      - type: "internal"
        name: "times-square"
        requiredScopes:
          - "exec:admin"
        template: "https://{{base_hostname}}/times-square/api"
        openapi: "https://{{base_hostname}}/times-square/api/openapi.json"
        docsUrl: "https://times-square.lsst.io/"
        versions:
          v1:
            template: "https://{{base_hostname}}/times-square/api/v1"
  slackAlerts: true
  tap:
    servers:
      ppdbtap:
        enabled: true
        schemaVersion: "releases/w.2026.35"
      ssotap:
        enabled: true
      tap:
        enabled: true
  useSubdomains:
    - "nublado"

cloudsql:
  enabled: true
  instanceConnectionName: "science-platform-int-dc5d:us-central1:science-platform-int-8f439af2"
  serviceAccount: "repertoire@science-platform-int-dc5d.iam.gserviceaccount.com"
