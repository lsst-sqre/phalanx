apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls
spec:
  secretName: tls

  issuerRef:
    name: "letsencrypt-dns"
    kind: ClusterIssuer

  subject:
    organizations:
      - "lsst.org"

  dnsNames:
    - {{ .Values.ingress.hostname }}
