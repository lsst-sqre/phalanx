apiVersion: v1
kind: Service
metadata:
  name: "schedview_prenight"
  labels:
    {{- include "schedview_prenight.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "schedview_prenight.selectorLabels" . | nindent 4 }}
