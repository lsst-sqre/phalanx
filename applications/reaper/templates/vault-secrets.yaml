apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: reaper-secret
  labels:
    {{- include "reaper.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/reaper"
  type: "Opaque"
