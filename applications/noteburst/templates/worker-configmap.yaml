apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "noteburst.fullname" . }}-worker
  labels:
    {{- include "noteburst.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
    {{- toYaml .Values.workerConfig | nindent 4 }}
