apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "noteburst.fullname" . }}
  labels:
    {{- include "noteburst.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
