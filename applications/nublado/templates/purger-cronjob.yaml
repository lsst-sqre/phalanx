{{if .Values.purger.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: purger
spec:
  schedule: {{ .Values.purger.schedule | quote }}
  successfulJobsHistoryLimit: 1
  concurrencyPolicy: "Forbid"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          automountServiceAccountToken: false
          {{- with .Values.purger.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.purger.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
          - name: "{{ .Chart.Name }}-purger"
            {{- if or .Values.controller.slackAlerts .Values.sentry.enabled }}
            env:
            {{- if .Values.controller.slackAlerts }}
            - name: "NUBLADO_ALERT_HOOK"
              valueFrom:
                secretKeyRef:
                  name: "nublado-secret"
                  key: "slack-webhook"
            {{- end }}
            {{- if .Values.sentry.enabled }}
            - name: "SENTRY_DSN"
              valueFrom:
                secretKeyRef:
                  name: "nublado-secret"
                  key: "sentry-dsn"
            - name: "SENTRY_ENVIRONMENT"
              value: {{ .Values.global.environmentName }}
            {{- end }}
            {{- end }}
            command: [ "nublado", "purger", "execute" ]
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            imagePullPolicy: "{{ .Values.image.pullPolicy }}"
            {{- with .Values.purger.resources }}
            resources:
              {{- toYaml . | nindent 14 }}
            {{- end }}
            securityContext:
              allowPrivilegeEscalation: true
              privileged: true
              runAsUser: 0
              runAsNonRoot: false
              readOnlyRootFilesystem: true
            volumeMounts:
            - mountPath: /purge
              name: {{ .Values.purger.volumeName | quote }}
            - mountPath: /etc/purger
              name: config
          # We need to extend this for PVCs too
          volumes:
          {{- range .Values.controller.config.lab.volumes }}
          - name: {{ .name | quote }}
          {{- if eq .source.type "nfs" }}
            nfs:
              server: {{ .source.server | quote }}
              path: {{ .source.serverPath | quote }}
          {{- else if eq .source.type "hostPath" }}
            hostPath:
              path: {{ .source.path | quote }}
          {{- end }}
          {{- end }}
          - name: config
            configMap:
              name: "purger-configmap"
{{- end }}
