apiVersion: v1
kind: Service
metadata:
  name: "herald"
  labels:
    {{- include "herald.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "herald.selectorLabels" . | nindent 4 }}
