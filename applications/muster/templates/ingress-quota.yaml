apiVersion: gafaelfawr.lsst.io/v1alpha1
kind: GafaelfawrIngress
metadata:
  name: "muster-quota"
  labels:
    {{- include "muster.labels" . | nindent 4 }}
config:
  scopes:
    all:
      - "read:image"
  service: "muster-quota"
template:
  metadata:
    name: "muster-quota"
    {{- with .Values.ingress.annotations }}
    annotations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  spec:
    rules:
      - host: {{ required "global.host must be set" .Values.global.host | quote }}
        http:
          paths:
            - path: "{{ .Values.config.pathPrefix }}/auth/quota"
              pathType: "Exact"
              backend:
                service:
                  name: "muster"
                  port:
                    number: 8080
