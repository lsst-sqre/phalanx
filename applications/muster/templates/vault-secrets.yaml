{{- if .Values.config.slackAlerts -}}
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: "muster"
  labels:
    {{- include "muster.labels" . | nindent 4 }}
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/muster"
  type: Opaque
{{- end }}
