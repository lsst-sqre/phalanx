apiVersion: v1
kind: Service
metadata:
  name: "muster"
  labels:
    {{- include "muster.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "muster.selectorLabels" . | nindent 4 }}
