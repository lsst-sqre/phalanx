apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: weaviate-api-key
  labels:
    {{- include "gai-helpers.labels" . | nindent 4 }}
spec:
  path: "{{- .Values.global.vaultSecretsPathPrefix }}/weaviate-api-key"
  type: kubernetes.io/dockerconfigjson