# Default values for so-evening-tailgate.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# -- Number of web deployment pods to start
replicaCount: 1

image:
  # -- Image to use in the so-evening-tailgate deployment
  repository: "ghcr.io/lsst-so/so-slackbot-evening-tailgate"

  # -- Pull policy for the so-evening-tailgate image
  pullPolicy: "IfNotPresent"

  # -- Tag of image to use
  # @default -- The appVersion of the chart
  tag: null

config:
  # -- Logging level
  logLevel: "INFO"

  # -- Logging profile (`production` for JSON, `development` for
  # human-friendly)
  logProfile: "production"

  # -- URL path prefix
  pathPrefix: "/so-evening-tailgate"

  # -- Whether to send Slack alerts for unexpected failures
  slackAlerts: false

ingress:
  # -- Additional annotations for the ingress rule
  annotations: {}

# -- Affinity rules for the so-evening-tailgate deployment pod
affinity: {}

# -- Node selection rules for the so-evening-tailgate deployment pod
nodeSelector: {}

# -- Annotations for the so-evening-tailgate deployment pod
podAnnotations: {}

# -- Resource limits and requests for the so-evening-tailgate deployment pod
# @default -- See `values.yaml`
resources: {}

# -- Tolerations for the so-evening-tailgate deployment pod
tolerations: []

# The following will be set by parameters injected by Argo CD and should not
# be set in the individual environment values files.
global:
  # -- Host name for ingress
  # @default -- Set by Argo CD
  host: null

  # -- Base URL for Repertoire discovery API
  # @default -- Set by Argo CD
  repertoireUrl: null

  # -- Base path for Vault secrets
  # @default -- Set by Argo CD
  vaultSecretsPath: null
