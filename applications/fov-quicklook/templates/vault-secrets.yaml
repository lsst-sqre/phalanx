{{- if .Values.use_vault }}
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: fov-quicklook
spec:
  path: "{{ .Values.global.vaultSecretsPath }}/fov-quicklook"
  type: Opaque
{{- end }}
