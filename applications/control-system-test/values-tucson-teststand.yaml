namespace: &ns control-system-test
imageTag: &imageTag c0029

csc_collector:
  namespace: *ns
  siteTag: tucson
  kafkaBrokerAddress: sasquatch-kafka-brokers.sasquatch:9092
  schemaRegistryUrl: http://sasquatch-schema-registry.sasquatch:8081
  s3EndpointUrl: https://s3.tu.lsst.org

  secrets:
  - name: nexus3-docker
    key: pull-secret
    type: kubernetes.io/dockerconfigjson
  - name: ts-salkafka
    key: ts/software/ts-salkafka
  - name: lfa
    key: ts/software/lfa

test42:
  enabled: true
  namespace: *ns
  image:
    repository: ts-dockerhub.lsst.org/test
    tag: *imageTag
    pullPolicy: Always
  env:
    RUN_ARG: 42
