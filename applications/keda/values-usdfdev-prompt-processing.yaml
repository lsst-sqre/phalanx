keda:

  podLabels:
    keda: {
      env: "dev"
    }
    metricsAdapter: {
      env: "dev"
    }
    webhooks: {
      env: "dev"
    }

  resources:
    operator:
      limits:
        cpu: 1
      requests:
        cpu: 1
    metricServer:
      limits:
        cpu: 1
      requests:
        cpu: 1
