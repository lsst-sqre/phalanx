replicaCount: 2

config:
  volumes:
    cleanupInterval: "1h"
    sharedWorkarea:
      nfs:
        path: "/firefly-share/shared-workarea"
        server: "10.234.16.4"
    privateWorkarea:
      pvc:
        size: "20Gi"
        storageClass: "standard-rwo"
  ssotap: "ssotap"

redis:
  persistence:
    enabled: true
    storageClass: "standard-rwo"
  resources:
    limits:
      memory: "128Mi"

resources:
  limits:
    memory: "2Gi"
