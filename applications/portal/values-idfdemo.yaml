replicaCount: 2

image:
  tag: "portal-2025.5.0-test1"

tolerations:
  - key: "kubernetes.io/arch"
    value: arm64
    operator: "Equal"
    effect: NoSchedule

nodeSelector:
  node_pool: "arm-core-pool"

config:
  alertMessage: |
    This is a test alert message.
    It should be shown to all users.
  volumes:
    cleanupInterval: "1h"
    sharedWorkarea:
      nfs:
        path: "/home-share/_reloc/firefly/shared-workarea"
        server: "10.235.176.36"
    privateWorkarea:
      pvc:
        size: "20Gi"
        storageClass: "standard-rwo"
  ssotap: "ssotap"
  hipsUrl: "https://data-dev.lsst.cloud/api/hips/v2/dp1/deep_coadd/band_r"

redis:
  persistence:
    enabled: true
    storageClass: "standard-rwo"
  resources:
    limits:
      memory: "128Mi"
  tolerations:
    - key: "kubernetes.io/arch"
      value: arm64
      operator: "Equal"
      effect: NoSchedule
  nodeSelector:
    node_pool: "arm-core-pool"

resources:
  limits:
    memory: "2Gi"
