config:
  livetap: "live"
  ssotap: "ssotap"

redis:
  persistence:
    enabled: true
    storageClass: "wekafs--sdf-k8s01"
  resources:
    limits:
      memory: "512Mi"

resources:
  limits:
    memory: "30Gi"
