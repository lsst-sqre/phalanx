replicaCount: 4

config:
  cleanupInterval: "1h"
  volumes:
    sharedWorkarea:
      nfs:
        path: "/firefly-share/shared-workarea"
        server: "10.91.240.5"
    privateWorkarea:
      pvc:
        size: "20Gi"
        storageClass: "standard-rwo"
  ssotap: "ssotap"

redis:
  persistence:
    enabled: true
    storageClass: "standard-rwo"

image:
  tag: "portal-test-2025.3-t7"

resources:
  limits:
    memory: "30Gi"
