apiVersion: v1
kind: Service
metadata:
  name: "conesearch"
  labels:
    {{- include "conesearch.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "conesearch.selectorLabels" . | nindent 4 }}
