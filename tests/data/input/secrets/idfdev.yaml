argocd:
  dex.clientSecret:
    value: null
gafaelfawr:
  cilogon-client-secret:
    description: >-
      Secret used to authenticate to CILogon as part of the OpenID Connect
      login protocol to obtain an identity token for the user. This secret
      can be changed at any time.
    value: "some-cilogon-password"
  database-password:
    value: "some-other-database-password"
  ldap-password:
    value: "some-ldap-password"
mobu:
  app-alert-webhook:
    value: "https://hooks.slack.com/app-slack-hook"
nublado:
  aws-credentials.ini:
    value: "aws-credentials"
  butler-gcs-idf-creds.json:
    value: "gcs-credentials"
  butler-hmac-idf-creds.json:
    value: "more-gcs-credentials"
  postgres-credentials.txt:
    value: "postgres-credentials"
