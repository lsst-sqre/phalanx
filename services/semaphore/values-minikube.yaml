semaphore:
  ingress:
    enabled: true
    hosts:
      - host: "minikube.lsst.codes"
        paths:
          - "/semaphore"
  imagePullSecrets:
    - name: "pull-secret"

pull-secret:
  enabled: true
  path: secret/k8s_operator/minikube.lsst.codes/pull-secret
